#pragma once
// Forward declarations for internal window types used by LoginOverlay
// This header is intentionally minimal — the actual MonixApp class
// is defined in main.cpp and LoginOverlay only needs basic types.
