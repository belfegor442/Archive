#pragma once

#include "../telemetry/Snapshot.hpp"
#include "SystemSnapshot.hpp"

namespace monix {

class SnapshotAdapter {
public:
  static SystemSnapshot FromLegacy(const Snapshot& legacy) {
    SystemSnapshot snap;
    snap.id = legacy.snapshotId;
    snap.timestampNs = legacy.collectedAtMonotonicNs;
    snap.wallTimeMs = legacy.collectedAtWallMs;
    snap.host = legacy.host;
    snap.uptimeSeconds = legacy.uptimeSeconds;
    snap.bootPhase = legacy.bootPhase;

    snap.cpu.pct = legacy.cpuPct;
    snap.cpu.kernelTimePct = legacy.kernelTimePct;
    snap.cpu.userTimePct = legacy.userTimePct;
    memcpy(snap.cpu.vendor, legacy.cpuVendor, sizeof(legacy.cpuVendor));
    memcpy(snap.cpu.brand, legacy.cpuBrand, sizeof(legacy.cpuBrand));
    snap.cpu.family = legacy.cpuFamily;
    snap.cpu.model = legacy.cpuModel;
    snap.cpu.stepping = legacy.cpuStepping;
    snap.cpu.cores = legacy.cpuCores;
    snap.cpu.logicalCpus = legacy.cpuLogicalCpus;
    snap.cpu.htEnabled = legacy.cpuHtEnabled;
    snap.cpu.tscPerSec = legacy.cpuTscPerSec;
    snap.cpu.microcodeRev = legacy.cpuMicrocodeRev;
    snap.cpu.featuresEdx = legacy.cpuFeaturesEdx;
    snap.cpu.featuresEcx = legacy.cpuFeaturesEcx;
    snap.cpu.extFeatures = legacy.cpuExtFeatures;
    snap.cpu.baseClockMhz = legacy.cpuBaseClockMhz;
    snap.cpu.estimatedFrequencyMhz = legacy.estimatedFrequencyMhz;
    snap.cpu.kernelUserRatio = legacy.kernelUserRatio;
    snap.cpu.tscDelta = legacy.cpuTscDelta;
    snap.cpu.ipcEstimate = legacy.ipcEstimate;
    snap.cpu.processorQueueLength = legacy.processorQueueLength;
    snap.cpu.contextSwitchesPerSec = legacy.contextSwitchesPerSec;
    snap.cpu.systemCallsPerSec = legacy.systemCallsPerSec;
    snap.cpu.interruptsPerSec = legacy.interruptsPerSec;
    snap.cpu.processorCount = legacy.processorCount;
    snap.cpu.totalContextSwitches = legacy.totalContextSwitches;
    snap.cpu.totalInterruptCount = legacy.totalInterruptCount;
    snap.cpu.totalDpcCount = legacy.totalDpcCount;
    snap.cpu.dpcTimePerSec = legacy.dpcTimePerSec;
    snap.cpu.totalIsrCount = legacy.totalIsrCount;
    snap.cpu.isrTimePerSec = legacy.isrTimePerSec;
    snap.cpu.suspendedThreadCount = legacy.suspendedThreadCount;
    snap.cpu.readyThreadCount = legacy.readyThreadCount;
    snap.cpu.waitingThreadCount = legacy.waitingThreadCount;
    snap.cpu.realtimeThreadCount = legacy.realtimeThreadCount;
    snap.cpu.highPriorityThreadCount = legacy.highPriorityThreadCount;
    snap.cpu.processorAffinities = legacy.processorAffinities;
    snap.cpu.threadCountDelta = legacy.threadCountDelta;
    snap.cpu.threadCreationDelta = legacy.threadCreationDelta;
    snap.cpu.threadTerminationDelta = legacy.threadTerminationDelta;

    snap.memory.usedBytes = legacy.ramUsedBytes;
    snap.memory.totalBytes = legacy.ramTotalBytes;
    snap.memory.availBytes = legacy.ramAvailBytes;
    snap.memory.commitUsedBytes = legacy.commitUsedBytes;
    snap.memory.commitLimitBytes = legacy.commitLimitBytes;
    snap.memory.commitPeakBytes = legacy.commitPeakBytes;
    snap.memory.kernelPoolPagedBytes = legacy.kernelPoolPagedBytes;
    snap.memory.kernelPoolNonpagedBytes = legacy.kernelPoolNonpagedBytes;
    snap.memory.systemCacheBytes = legacy.systemCacheBytes;
    snap.memory.pageFaultsDelta = legacy.pageFaultsDelta;
    snap.memory.hardPageFaultsDelta = legacy.hardPageFaultsDelta;
    snap.memory.standbyListBytes = legacy.standbyListBytes;
    snap.memory.modifiedListBytes = legacy.modifiedListBytes;
    snap.memory.freeListBytes = legacy.freeListBytes;
    snap.memory.zeroListBytes = legacy.zeroListBytes;
    snap.memory.workingSetTotalBytes = legacy.workingSetTotalBytes;
    snap.memory.totalWorkingSetBytes = legacy.totalWorkingSetBytes;
    snap.memory.pageFileUsedBytes = legacy.pageFileUsedBytes;
    snap.memory.pageFileTotalBytes = legacy.pageFileTotalBytes;

    snap.gpu.pct = legacy.gpuPct;
    snap.gpu.pctValid = legacy.gpuPctValid;
    snap.gpu.tempC = legacy.gpuTempC;
    snap.gpu.tempEstimated = legacy.gpuTempEstimated;
    snap.gpu.model = legacy.gpuModel;
    snap.gpu.driverVersion = legacy.gpuDriverVersion;
    snap.gpu.vramTotalBytes = legacy.gpuVramTotalBytes;
    snap.gpu.vramUsedBytes = legacy.gpuVramUsedBytes;
    snap.gpu.powerWatts = legacy.gpuPowerWatts;
    snap.gpu.fanRpm = legacy.gpuFanRpm;
    snap.gpu.frameTimeMs = legacy.frameTimeMs;
    snap.gpu.tdrLevel = legacy.tdrLevel;

    snap.storage.readBytesPerSec = legacy.diskReadBytesPerSec;
    snap.storage.writeBytesPerSec = legacy.diskWriteBytesPerSec;
    snap.storage.totalBytes = legacy.diskTotalBytes;
    snap.storage.freeBytes = legacy.diskFreeBytes;
    snap.storage.pctUsed = legacy.diskPctUsed;
    snap.storage.queueLength = legacy.diskQueueLength;
    snap.storage.readLatencyMs = legacy.diskReadLatencyMs;
    snap.storage.writeLatencyMs = legacy.diskWriteLatencyMs;
    snap.storage.readIops = legacy.diskReadIops;
    snap.storage.writeIops = legacy.diskWriteIops;
    snap.storage.smartHealthOk = legacy.smartHealthOk;
    snap.storage.nvmeTempC = legacy.nvmeTempC;
    snap.storage.nvmeTempValid = legacy.nvmeTempValid;
    snap.storage.tempC = legacy.storageTempC;
    snap.storage.tempEstimated = legacy.storageTempEstimated;

    snap.network.upBytesPerSec = legacy.netUpBytesPerSec;
    snap.network.downBytesPerSec = legacy.netDownBytesPerSec;
    snap.network.inboundConnections = legacy.inboundConnections;
    snap.network.outboundConnections = legacy.outboundConnections;
    snap.network.udpConnectionCount = legacy.udpConnectionCount;
    snap.network.dnsPseudo = legacy.dnsPseudo;
    snap.network.latencyMs = legacy.latencyMs;
    snap.network.latencySource = legacy.latencySource;
    snap.network.pingRttMs = legacy.pingRttMs;
    snap.network.dnsResolutionMs = legacy.dnsResolutionMs;
    snap.network.dnsResolutionOk = legacy.dnsResolutionOk;
    snap.network.tcpRetransmits = legacy.tcpRetransmits;
    snap.network.tcpResets = legacy.tcpResets;
    snap.network.routeTableHash = legacy.routeTableHash;
    snap.network.proxyEnabled = legacy.proxyEnabled;
    snap.network.adapterCount = legacy.netAdapterCount;
    snap.network.primaryLinkSpeedBps = legacy.netPrimaryLinkSpeedBps;
    snap.network.adapterAddresses = legacy.netAdapterAddresses;
    snap.network.adapterGateways = legacy.netAdapterGateways;
    snap.network.adapterSpeeds = legacy.netAdapterSpeeds;
    snap.network.adapterOperStatuses = legacy.netAdapterOperStatuses;
    snap.network.adapterTypes = legacy.netAdapterTypes;
    snap.network.vpnAdapterDescriptions = legacy.vpnAdapterDescriptions;
    snap.network.flows = legacy.flows;

    snap.processes.list = legacy.processes;
    snap.processes.count = legacy.processCount;
    snap.processes.threadCount = legacy.threadCount;
    snap.processes.handleCount = legacy.handleCount;

    snap.power.acLineStatus = legacy.acLineStatus;
    snap.power.batteryFlag = legacy.batteryFlag;
    snap.power.batteryLifePercent = legacy.batteryLifePercent;
    snap.power.batteryLifeTimeSec = legacy.batteryLifeTimeSec;
    snap.power.batteryChargeRate = legacy.batteryChargeRate;
    snap.power.batteryChargeState = legacy.batteryChargeState;
    snap.power.batteryChargePercent = legacy.batteryChargePercent;
    snap.power.batteryWearLevel = legacy.batteryWearLevel;
    snap.power.batteryCycleCount = legacy.batteryCycleCount;
    snap.power.batteryTemperature = legacy.batteryTemperature;
    snap.power.powerPlanIndex = legacy.powerPlanIndex;
    snap.power.powerPlanGuid = legacy.powerPlanGuid;
    snap.power.powerSaverActive = legacy.powerSaverActive;
    snap.power.highPerfActive = legacy.highPerfActive;
    snap.power.balancedActive = legacy.balancedActive;
    snap.power.modernStandbyActive = legacy.modernStandbyActive;
    snap.power.sleepStateActive = legacy.sleepStateActive;
    snap.power.hibernateActive = legacy.hibernateActive;
    snap.power.cpuPackagePowerCap = legacy.cpuPackagePowerCap;
    snap.power.systemPowerCap = legacy.systemPowerCap;
    snap.power.idlePowerDrawHigh = legacy.idlePowerDrawHigh;

    snap.thermal.cpuCoreTempC = legacy.cpuCoreTempC;
    snap.thermal.cpuCoreTempMax = legacy.cpuCoreTempMax;
    snap.thermal.motherboardTempC = legacy.motherboardTempC;
    snap.thermal.vrmTempC = legacy.vrmTempC;
    snap.thermal.ambientTempC = legacy.ambientTempC;
    snap.thermal.cpuThrottleTempC = legacy.cpuThrottleTempC;
    snap.thermal.cpuThrottling = legacy.cpuThrottling;
    snap.thermal.fanCount = legacy.fanCount;
    snap.thermal.fanSpeeds = legacy.fanSpeeds;
    snap.thermal.pumpSpeed = legacy.pumpSpeed;
    snap.thermal.pumpPresent = legacy.pumpPresent;
    snap.thermal.thermalSensorCount = legacy.thermalSensorCount;
    snap.thermal.thermalSensorFailures = legacy.thermalSensorFailures;
    snap.thermal.thermalTrendC = legacy.thermalTrendC;
    snap.thermal.thermalRecoveryCount = legacy.thermalRecoveryCount;
    snap.thermal.thermalHeatSoakIndex = legacy.thermalHeatSoakIndex;
    snap.thermal.coolingCurveSlope = legacy.coolingCurveSlope;

    snap.security.selfSignatureValid = legacy.selfSignatureValid;
    snap.security.selfHashComputed = legacy.selfHashComputed;
    snap.security.unsignedDriverCount = legacy.unsignedDriverCount;
    snap.security.unsignedDriverNames = legacy.unsignedDriverNames;
    snap.security.suspiciousScriptHosts = legacy.suspiciousScriptHosts;
    snap.security.uacConsentProcesses = legacy.uacConsentProcesses;
    snap.security.lsassAccessCount = legacy.lsassAccessCount;
    snap.security.debugPortActive = legacy.debugPortActive;
    snap.security.vmIndicators = legacy.vmIndicators;
    snap.security.hookModulesDetected = legacy.hookModulesDetected;
    snap.security.peHeaderTamper = legacy.peHeaderTamper;
    snap.security.scheduledTaskCount = legacy.scheduledTaskCount;
    snap.security.suspiciousModules = legacy.suspiciousModules;
    snap.security.processPaths = legacy.processPaths;
    snap.security.selfExePath = legacy.selfExePath;

    snap.filesystem.system32FileCount = legacy.fsSystem32FileCount;
    snap.filesystem.system32HiddenCount = legacy.fsSystem32HiddenCount;
    snap.filesystem.system32SystemCount = legacy.fsSystem32SystemCount;
    snap.filesystem.driversFileCount = legacy.fsDriversFileCount;
    snap.filesystem.driversHiddenCount = legacy.fsDriversHiddenCount;
    snap.filesystem.recycleBinContentCount = legacy.fsRecycleBinContentCount;
    snap.filesystem.programFilesCount = legacy.fsProgramFilesCount;
    snap.filesystem.volumeDirtyBit = legacy.fsVolumeDirtyBit;
    snap.filesystem.volumeLabel = legacy.fsVolumeLabel;
    snap.filesystem.volumeSerial = legacy.fsVolumeSerial;
    snap.filesystem.mountPointCount = legacy.fsMountPointCount;
    snap.filesystem.reparsePointCount = legacy.fsReparsePointCount;
    snap.filesystem.sparseFileCount = legacy.fsSparseFileCount;
    snap.filesystem.adsWithDataCount = legacy.fsAdsWithDataCount;
    snap.filesystem.integrityFailures = legacy.fsIntegrityFailures;
    snap.filesystem.usnJournalId = legacy.fsUsnJournalId;
    snap.filesystem.lastUsn = legacy.fsLastUsn;
    snap.filesystem.corruptionWarnings = legacy.fsCorruptionWarnings;
    snap.filesystem.chkdskPending = legacy.fsChkdskPending;

    snap.registry.run.keyCount = legacy.regKeyCountRun;
    snap.registry.run.valueCount = legacy.regValueCountRun;
    snap.registry.run.hash = legacy.regHashRun;
    snap.registry.runOnce.keyCount = legacy.regKeyCountRunOnce;
    snap.registry.runOnce.valueCount = legacy.regValueCountRunOnce;
    snap.registry.runOnce.hash = legacy.regHashRunOnce;
    snap.registry.shell.keyCount = legacy.regKeyCountShell;
    snap.registry.shell.valueCount = legacy.regValueCountShell;
    snap.registry.shell.hash = legacy.regHashShell;
    snap.registry.policies.keyCount = legacy.regKeyCountPolicies;
    snap.registry.policies.valueCount = legacy.regValueCountPolicies;
    snap.registry.policies.hash = legacy.regHashPolicies;
    snap.registry.services.keyCount = legacy.regKeyCountServices;
    snap.registry.services.valueCount = legacy.regValueCountServices;
    snap.registry.services.hash = legacy.regHashServices;
    snap.registry.drivers.keyCount = legacy.regKeyCountDrivers;
    snap.registry.drivers.valueCount = legacy.regValueCountDrivers;
    snap.registry.drivers.hash = legacy.regHashDrivers;
    snap.registry.firewall.keyCount = legacy.regKeyCountFirewall;
    snap.registry.firewall.valueCount = legacy.regValueCountFirewall;
    snap.registry.firewall.hash = legacy.regHashFirewall;
    snap.registry.uac.keyCount = legacy.regKeyCountUac;
    snap.registry.uac.valueCount = legacy.regValueCountUac;
    snap.registry.uac.hash = legacy.regHashUac;
    snap.registry.taskSched.keyCount = legacy.regKeyCountTaskSched;
    snap.registry.taskSched.valueCount = legacy.regValueCountTaskSched;
    snap.registry.taskSched.hash = legacy.regHashTaskSched;
    snap.registry.com.keyCount = legacy.regKeyCountCom;
    snap.registry.com.valueCount = legacy.regValueCountCom;
    snap.registry.com.hash = legacy.regHashCom;
    snap.registry.telemetry.keyCount = legacy.regKeyCountTelemetry;
    snap.registry.telemetry.valueCount = legacy.regValueCountTelemetry;
    snap.registry.telemetry.hash = legacy.regHashTelemetry;
    snap.registry.audit.keyCount = legacy.regKeyCountAudit;
    snap.registry.audit.valueCount = legacy.regValueCountAudit;
    snap.registry.audit.hash = legacy.regHashAudit;
    snap.registry.env.keyCount = legacy.regKeyCountEnv;
    snap.registry.env.valueCount = legacy.regValueCountEnv;
    snap.registry.env.hash = legacy.regHashEnv;
    snap.registry.path.keyCount = legacy.regKeyCountPath;
    snap.registry.path.valueCount = legacy.regValueCountPath;
    snap.registry.path.hash = legacy.regHashPath;
    snap.registry.appAssoc.keyCount = legacy.regKeyCountAppAssoc;
    snap.registry.appAssoc.valueCount = legacy.regValueCountAppAssoc;
    snap.registry.appAssoc.hash = legacy.regHashAppAssoc;
    snap.registry.shellExt.keyCount = legacy.regKeyCountShellExt;
    snap.registry.shellExt.valueCount = legacy.regValueCountShellExt;
    snap.registry.shellExt.hash = legacy.regHashShellExt;
    snap.registry.defApp.keyCount = legacy.regKeyCountDefApp;
    snap.registry.defApp.valueCount = legacy.regValueCountDefApp;
    snap.registry.defApp.hash = legacy.regHashDefApp;
    snap.registry.fileAssoc.keyCount = legacy.regKeyCountFileAssoc;
    snap.registry.fileAssoc.valueCount = legacy.regValueCountFileAssoc;
    snap.registry.fileAssoc.hash = legacy.regHashFileAssoc;
    snap.registry.configFile.keyCount = legacy.regKeyCountConfigFile;
    snap.registry.configFile.valueCount = legacy.regValueCountConfigFile;
    snap.registry.configFile.hash = legacy.regHashConfigFile;
    snap.registry.startupFolderCount = legacy.startupFolderCount;
    snap.registry.envVarCount = legacy.envVarCount;
    snap.registry.regHashEnvPath = legacy.regHashEnvPath;

    snap.audio.outputDeviceCount = legacy.audioOutputDeviceCount;
    snap.audio.inputDeviceCount = legacy.audioInputDeviceCount;
    snap.audio.mixerCount = legacy.audioMixerCount;
    snap.audio.masterVolume = legacy.audioMasterVolume;
    snap.audio.masterMuted = legacy.audioMasterMuted;
    snap.audio.masterVolumeLeft = legacy.audioMasterVolumeLeft;
    snap.audio.masterVolumeRight = legacy.audioMasterVolumeRight;
    snap.audio.sampleRate = legacy.audioSampleRate;
    snap.audio.bitsPerSample = legacy.audioBitsPerSample;
    snap.audio.channels = legacy.audioChannels;
    snap.audio.waveOutOpen = legacy.audioWaveOutOpen;
    snap.audio.waveInOpen = legacy.audioWaveInOpen;
    snap.audio.serviceRunning = legacy.audioServiceRunning;
    snap.audio.micMuted = legacy.audioMicMuted;
    snap.audio.systemMuted = legacy.audioSystemMuted;
    snap.audio.headphoneJack = legacy.audioHeadphoneJack;
    snap.audio.spatialization = legacy.audioSpatialization;
    snap.audio.playbackActive = legacy.audioPlaybackActive;
    snap.audio.recordingActive = legacy.audioRecordingActive;
    snap.audio.deviceHash = legacy.audioDeviceHash;
    snap.audio.formatHash = legacy.audioFormatHash;
    snap.audio.latencyMs = legacy.audioLatencyMs;
    snap.audio.codecEvents = legacy.audioCodecEvents;
    snap.audio.decoderErrors = legacy.audioDecoderErrors;

    snap.display.refreshRateHz = legacy.displayRefreshRateHz;
    snap.display.width = legacy.displayWidth;
    snap.display.height = legacy.displayHeight;
    snap.display.bitsPerPel = legacy.displayBitsPerPel;
    snap.display.monitorCount = legacy.displayMonitorCount;
    snap.display.hdrEnabled = legacy.hdrEnabled;
    snap.display.vsyncEnabled = legacy.vsyncEnabled;
    snap.display.desktopCompositionEnabled = legacy.desktopCompositionEnabled;
    snap.display.displayNames = legacy.displayNames;

    snap.kernel.systemTime100ns = legacy.systemTime100ns;
    snap.kernel.uptimeMs = legacy.uptimeMs;
    snap.kernel.driverCount = legacy.driverCount;
    snap.kernel.driverNames = legacy.driverNames;
    snap.kernel.totalHandles = legacy.totalHandles;
    snap.kernel.totalObjects = legacy.totalObjects;
    snap.kernel.totalSyscalls = legacy.totalSyscalls;
    snap.kernel.ntStatusErrors = legacy.ntStatusErrors;
    snap.kernel.registryOps = legacy.registryOps;
    snap.kernel.sessionCount = legacy.sessionCount;
    snap.kernel.currentSessionId = legacy.currentSessionId;
    snap.kernel.ioReadBytesDelta = legacy.ioReadBytesDelta;
    snap.kernel.ioWriteBytesDelta = legacy.ioWriteBytesDelta;
    snap.kernel.pnpDeviceCount = legacy.pnpDeviceCount;
    snap.kernel.timeChangeDetected = legacy.timeChangeDetected;
    snap.kernel.processCount = legacy.processCount;
    snap.kernel.threadCount = legacy.threadCount;
    snap.kernel.handleCount = legacy.handleCount;

    snap.reliability.sehExceptionCount = legacy.sehExceptionCount;
    snap.reliability.unhandledExceptionCount = legacy.unhandledExceptionCount;
    snap.reliability.accessViolationCount = legacy.accessViolationCount;
    snap.reliability.heapCorruptionDetected = legacy.heapCorruptionDetected;
    snap.reliability.assertionFailureCount = legacy.assertionFailureCount;
    snap.reliability.stackOverflowCount = legacy.stackOverflowCount;
    snap.reliability.deadlockRecoveryCount = legacy.deadlockRecoveryCount;
    snap.reliability.processRestartCount = legacy.processRestartCount;
    snap.reliability.moduleReloadCount = legacy.moduleReloadCount;
    snap.reliability.uiFreezeDetected = legacy.uiFreezeDetected;
    snap.reliability.hangDetected = legacy.hangDetected;
    snap.reliability.timeoutExceeded = legacy.timeoutExceeded;
    snap.reliability.retryStormDetected = legacy.retryStormDetected;
    snap.reliability.backoffEscalation = legacy.backoffEscalation;
    snap.reliability.circuitBreakerOpen = legacy.circuitBreakerOpen;
    snap.reliability.circuitBreakerClose = legacy.circuitBreakerClose;
    snap.reliability.fallbackModeEntry = legacy.fallbackModeEntry;
    snap.reliability.fallbackModeExit = legacy.fallbackModeExit;
    snap.reliability.configRollbackDetected = legacy.configRollbackDetected;
    snap.reliability.safeModeActive = legacy.safeModeActive;
    snap.reliability.telemetryDropDetected = legacy.telemetryDropDetected;
    snap.reliability.watchdogResetDetected = legacy.watchdogResetDetected;
    snap.reliability.serviceRecoveryAction = legacy.serviceRecoveryAction;
    snap.reliability.crashEventsToday = legacy.crashEventsToday;
    snap.reliability.exceptionLogCount = legacy.exceptionLogCount;
    snap.reliability.processHash = legacy.processHash;
    snap.reliability.moduleHash = legacy.moduleHash;
    snap.reliability.mainThreadResponsive = legacy.mainThreadResponsive;
    snap.reliability.uiResponsivenessMs = legacy.uiResponsivenessMs;
    snap.reliability.threadHealthOk = legacy.threadHealthOk;

    snap.hardware.smbiosHash = legacy.smbiosHash;
    snap.hardware.acpiHash = legacy.acpiHash;
    snap.hardware.biosVersion = legacy.biosVersion;
    snap.hardware.biosManufacturer = legacy.biosManufacturer;
    snap.hardware.biosMajorVer = legacy.biosMajorVer;
    snap.hardware.biosMinorVer = legacy.biosMinorVer;
    snap.hardware.voltage12V = legacy.voltage12V;
    snap.hardware.voltage5V = legacy.voltage5V;
    snap.hardware.voltage33V = legacy.voltage33V;
    snap.hardware.voltageVcore = legacy.voltageVcore;
    snap.hardware.voltageVram = legacy.voltageVram;
    snap.hardware.tpmPresent = legacy.tpmPresent;
    snap.hardware.tpmReady = legacy.tpmReady;
    snap.hardware.tpmVersion = legacy.tpmVersion;
    snap.hardware.cmosBatteryOk = legacy.cmosBatteryOk;
    snap.hardware.sensorPollFailures = legacy.sensorPollFailures;
    snap.hardware.pcieErrors = legacy.pcieErrors;
    snap.hardware.usbResets = legacy.usbResets;
    snap.hardware.sataResets = legacy.sataResets;
    snap.hardware.thunderboltEvents = legacy.thunderboltEvents;
    snap.hardware.ecEvents = legacy.ecEvents;
    snap.hardware.boardTempHotspot = legacy.boardTempHotspot;

    return snap;
  }
};

}
