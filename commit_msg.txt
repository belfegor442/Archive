Phase 2: Fix staging rollback/verify, path traversal, crash recovery

- StagingManager: rollback_staging now restores BackupAndReplace backups
- StagingManager: finalize_staging verifies copy via size+checksum after write
- StagingManager: backup files cleaned after successful finalize
- FileUtils: sanitize_relative_path blocks .. traversal in relative paths
- FileUtils: is_path_within validates canonical path containment
- StorageManager: store_file_in_dir rejects traversal in relative_path
- StagingManager: stage_file_in_dir rejects traversal in relative_path
- Application: recovery handles Finalizing/Staged/Preparing states with restore
- 6 new production tests: path traversal, rollback backup, verify, cleanup
- 147/147 tests passing
