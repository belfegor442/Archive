fix(build): embed longPathAware manifest for Windows MAX_PATH support

- archive-gui.manifest declares <longPathAware>true</longPathAware>
- Embedded via .rc resource in both GUI and CLI executables
- Enables paths up to 32767 chars on Windows 10 1607+
- Fixes crash when importing deeply nested folders (e.g. Monix with zig-dist)
