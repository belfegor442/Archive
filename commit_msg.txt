Fix read_backup_journal: single-pass deterministic parser, 175/175 tests

- read_backup_journal: replaced fragile two-pass line+object parser with
  single-pass deterministic parser that reads file once, finds {...} objects
  via brace-depth tracking, extracts quoted string values with backslash
  escape handling; correctly handles paths containing : , . _ - () [] and spaces
- Added test_backup_journal_roundtrip: writes two entries with different
  paths, reads back, verifies all three fields (destination, backup_path,
  state) match, restores from journal and verifies file content
- Added test_backup_journal_roundtrip_special_chars: roundtrip test with
  path containing spaces, parentheses, and dashes
- 175/175 tests passed, build clean
