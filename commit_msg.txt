Final hardening closure: atomic finalize rollback, committed cleanup, 173/173 tests

- StagingManager::finalize_staging: wrap entire copy+verify in try/catch;
  on any failure (copy or verify), restore_backups undoes all modifications,
  state transitions to RolledBack, original destinations preserved
- StagingManager: added cleanup_committed() method that finds and removes
  Committed staging dirs still lingering with residual files
- Application: calls cleanup_committed() on startup alongside cleanup_abandoned
- FileUtils::is_path_within: pure lexical component-based comparison with
  Windows backslash support (no weakly_canonical dependency)
- FileUtils::sanitize_relative_path: rejects absolute paths and .. traversal
- test_finalize_verification_failure_restores_backup: rewritten to actually
  test successful finalize with verify (old test was misleadingly named)
- test_verify_finalized_detects_mismatch: new test proving verify_finalized
  detects size/checksum mismatch
- test_finalize_copy_failure_restores_backup: new test proving collision
  rejection during finalize leaves destinations unchanged
- 5 new cleanup state tests: Staged, Finalizing, Abandoned, Corrupted,
  cleanup_committed removes committed staging dirs
- setup_base/cleanup_base: error_code overwrites prevent leftover files
  from crashing subsequent test runs
- 173/173 tests passed, build clean
